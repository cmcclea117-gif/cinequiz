feat: taste persona, batched AI generation, UX polish

worker.js
- Add PATCH /gist/:id support for updating existing Gists
- Add answer-type detection to /suggest: director/actor/character/
  author/artist/album/song/developer/showrunner questions now get
  correctly typed suggestions instead of defaulting to titles

movie-quiz.html
- Taste persona: "See your taste profile" button in Quiz Ready overlay
  generates a 2-4 word Myers-Briggs-style persona (e.g. "The Emotive
  Thrill Seeker") using type-aware AI prompt; PATCHes existing Gist so
  quiz takers see it above the questions; styled as display-font title
- Batch AI generation: wrong answers now generated in batches of 5
  (~16s for 20 questions vs ~60s before); falls back to individual
  calls if batch parse fails; fun rotating messages replace "batch X/Y"
- Taste persona on shared quiz: shown above questions with "TASTE
  PERSONA" eyebrow label; loaded from Gist tasteProfile field
- Short link + persistent Gist URL: store _currentGistId on publish,
  use SHA-less raw URL so PATCH updates are served without link changes
- iOS zoom fix: creator inputs set to font-size 16px; placeholder
  text 12px to fit narrow column; placeholders shortened to "Answer…"
  and "Hint (optional)"
- Persona display: styled in display font italic, strips AI-added quotes